# 新青年AI社团主理人 · 自我介绍

这是自我介绍网站的源码。网页入口是 `dist/index.html`，样式和交互分别在 `dist/collage.css`、`dist/collage.js`；所用照片也在 `dist` 中。

获得仓库写入权限的朋友可以在 Codex 中打开此仓库，修改 `dist` 内的文件，提交并推送到 `main` 分支。GitHub Actions 会自动把新版本发布到同一个网址。

首次发布需要在仓库 Settings → Pages 中把 Build and deployment → Source 设为 GitHub Actions。